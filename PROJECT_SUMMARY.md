# FormulaSnip V4 交付摘要

## 产品目标

FormulaSnip 是只识别单个数学公式的 Windows 本地桌面工具，面向论文写作中的高频操作：

```text
启动设置/教程 → 点击悬浮球截图 → MathCraft CPU 本地识别 → 校对电子公式 → 复制到 Word/MathType
```

产品不做正文 OCR、表格、整页布局分析和批量 PDF 解析。用户主动框选单个公式，因此无需公式检测模型。

## 当前实现

- 1180×760 的设置中心：常规、识别、悬浮球和使用方法；
- 深色/浅色主题、自定义悬浮球圆环颜色和中心 Logo；
- 置顶悬浮球截图、后台识别、电子公式预览、LaTeX/MathML 复制；
- MathCraft OCR 0.3.1 单引擎，固定使用 ONNX Runtime CPU；
- MathCraft 运行在可复用的隔离子进程中，通过共享内存传图；
- 识别输出继续执行语法质量检查和预览能力检查，并向用户显示校对提示；
- 旧的 `auto`、`rapid`、`paddle` 及未知识别偏好会自动迁移并持久化为 `mathcraft`；
- Windows 发行包内置并完整校验公式模型，首次使用可离线识别；源码模式或内置模型损坏时可能回退下载；
- 提供 Windows Setup 安装器、应用内更新、benchmark CLI 和带来源校验的合成样本。

## 选型与边界

MathCraft 提供适合本产品的公式专用 ONNX CPU 路径、模型校验和独立缓存，同时避免在产品层维护多引擎候选竞争。单引擎收敛不代表公式 OCR 已达到无需校对的准确率；所有识别结果仍需对照原图检查。

现有合成 benchmark 只用于验证执行路径和回归机制，不能用来宣称总体准确率。下一步应收集 100–300 个许可明确、覆盖真实使用分布的公式样本，报告字符串 exact、渲染等价、人工修正率、p50/p95 延迟和峰值内存。

当前 Windows 发行包捆绑固定模型权重。仍需在无 Python 的干净 Windows 环境扩大对离线首次识别、模型损坏回退、MathCraft 子进程、异 DPI 多显示器和 Word/MathType 剪贴板交换的实机验收。
