# Task Plan: FormulaSnip 软件设置中心 V4

## Goal
将现有紧凑设置弹窗升级为完整的软件设置中心，提供专业的信息架构、深浅主题切换、悬浮球实时预览、任意圆环颜色和自定义 Logo，同时保持高频悬浮识别流程简洁。

## Phases
- [x] Phase 1: 第一版原型、Word/MathType 输出与基础验证
- [x] Phase 2: 调研候选模型、许可证、体积和 Windows CPU 部署路径
- [x] Phase 3: 建立可扩展、带许可与 hash 校验的基准骨架和最小合成样本
- [x] Phase 4: 设计并实现图像诊断、输出质量评估和分层后端策略
- [x] Phase 5: 实测回归、独立审查并处理发现
- [x] Phase 6: 整理优化报告、运行方式和下一轮模型选型建议
- [x] Phase 7: 复核 V3 启动、设置、教程、截图和结果状态设计
- [x] Phase 8: 实现单悬浮工作流、持久设置与移除 pix2tex
- [x] Phase 9: 补充交互测试、生成预览并完成 Windows 启动验收
- [x] Phase 10: 更新文档与交付摘要
- [x] Phase 11: 设计 V4 软件级设置中心与主题系统
- [x] Phase 12: 实现大尺寸导航布局、圆环颜色盘与自定义 Logo
- [x] Phase 13: 补充测试、生成深浅主题预览并完成独立审查
- [x] Phase 14: 更新交付说明并启动 V4

## Key Questions
1. RapidLaTeXOCR、pix2tex、UniMERNet、PP-FormulaNet 等候选在速度、准确度、体积和许可上如何取舍？
2. 图片裁边、放大、灰度/对比度处理是否能稳定改善现有模型，哪些情况反而会变差？
3. 如何在不让每次识别都跑多个重模型的情况下识别“困难样本”并升级到高精度后端？
4. 在用户仍希望约 0.8 GB 安装体积时，第二个模型是否值得内置，还是应作为可选精确模式下载？

## Decisions Made
- 项目名使用 `FormulaSnip`，定位为单公式截图/图片识别工具。
- 第一版不做整页文字 OCR、表格识别或版面分析。
- 使用 Python + PySide6 构建 Windows 桌面原型。
- 识别器采用适配器结构；默认使用 RapidLaTeXOCR（Python 3.12 + CPU ONNX）。V3 已移除早期保留的 pix2tex 适配器。
- 优先插入 Word 原生公式；MathType 首版通过复制 LaTeX/MathML 兼容，不依赖私有 SDK。
- Word 首版使用 `OMaths.Add` + `BuildUp`，并明确仅保证经过验证的常见线性公式子集。
- 第二版先以可复现基准决定是否引入新模型；不以“模型数量更多”代替准确率证据。
- 默认体验优先考虑单模型快速路径；候选高精度模型仅在困难样本或用户选择“精确模式”时运行。
- V3 不再提供完整编辑器；右键菜单只保留“打开设置”和“退出软件”。
- 启动时显示统一设置面板；教程用分步对话呈现，用户点击“开始识别公式”后进入悬浮模式。
- 设置面板提供识别模式、悬浮球配色、面板主题、开机时显示设置以及教程入口。
- 任意复制成功后自动收起结果面板，让用户直接回到 Word/MathType 粘贴。
- 移除 pix2tex 产品入口和依赖，只保留 Rapid 与 PP-FormulaNet-S 两个互补后端。
- V4 设置中心采用大尺寸桌面窗口与左侧导航，不再沿用单列插件式弹窗。
- 悬浮球主体使用稳定的品牌底色，中间显示默认 `fx` Logo；颜色设置仅作用于外圈。
- 外圈颜色通过系统颜色盘选择；Logo 可上传常见图片格式并恢复默认，不保存图片内容副本，只保存用户选择的绝对路径。
- 软件深浅主题使用顶部单按钮切换，并同时作用于设置中心与结果面板。

## Errors Encountered
- 当前工作目录不是 Git 仓库；将在新项目目录初始化独立仓库。
- 本机尚未安装 PySide6、ONNX Runtime 或公式识别包；Pillow、pywin32、pytest 已存在。
- `uv sync` 的可编辑安装在当前中文父目录生成绝对路径 `.pth` 后，Python 未加载该路径；改用项目根目录下的平铺 `formulasnip/` 包结构，并让 pytest 显式加入项目根目录。开发启动统一使用 `python -m formulasnip`，最终 EXE 不依赖 `.pth`。
- `rapid-latex-ocr==0.0.9` 的源码导入 `requests`，但 PyPI 元数据没有声明该依赖；已在项目的 `rapid` extra 中显式补充。
- 直接按文件路径执行 `scripts/render_preview.py` 时同样不会自动加入项目根目录；预览脚本已显式从自身位置解析并加入根目录。
- 离屏界面渲染发现 PySide6 `QMargins` 没有 `topLeft()`/`bottomRight()`；已改为显式计算四边边距对应的 `QSize`。
- 从 `PySide6` 元包切换为 `PySide6-Essentials` 后，uv 卸载 Addons 时删除了两者共享的 Qt 文件，现有环境出现 `QtGui` DLL 加载失败；将强制重装 Essentials。全新安装不会经历该重叠卸载过程。
- 第一轮独立审查发现 Word 写入失败缺少回滚、`\\left`/`\\right` 前缀会误伤箭头命令、图片像素检查发生在解码后、截图浮层关闭可能不恢复主窗口、MathML 剪贴板缺少 MIME 类型；已进入修复和回归测试。
- 审查报告曾将 `OMaths.Add` 的返回值判断为 `OMath`；复核 Microsoft 官方文档后确认实际返回 `Range`，因此保留 `returned_range.OMaths(1).BuildUp()` 调用方式，只补强异常包装和回滚。
- 修复完成后新增 Word mock、截图生命周期、图片预检、MathML MIME 和 LaTeX 箭头回归测试；最终共 18 项测试通过，Ruff 检查通过，界面预览重新生成成功。
- 首轮预处理消融脚本直接把 `PIL.Image` 传给 RapidLaTeXOCR，触发其输入类型限制（只接受路径、NumPy、bytes）；改为传入 RGB NumPy 数组后继续测试。该限制已经由现有后端通过 PNG bytes 适配。
- 预处理消融显示：常规阈值/锐化/放大没有修复困难积分样例；0.65 倍缩小反而触发 512-token 退化输出并耗时约 59.6 秒。结论是不做无条件多预处理，增加解码上限/异常输出防护，并把处理限定为风险诊断触发。
- 本机匿名 GitHub REST API 查询触发共享出口 IP 的 rate limit；社区历史调研改由公开仓库网页、raw 文件、搜索结果及可用的独立来源交叉核验，不使用或暴露用户凭据。
- PP-FormulaNet-S 隔离环境首轮安装成功但构造 predictor 失败：`paddleocr`/`paddlex` 的公式后处理实际需要 `tokenizers`，默认最小依赖未自动带入。将其显式加入候选后端 extra 后继续实测，这也是安装完整性风险证据。
- 补入 `tokenizers` 后模型可在约 9.66 秒内构造，但预测后处理再次因未声明的 `ftfy` 缺失而失败；候选 extra 还需显式加入 `ftfy`，Paddle 公式模块的可选依赖完整性低于预期。
- PP-FormulaNet-S 加入 `ftfy` 后完成本机 CPU 推理：warm 约 0.76 秒；积分样例优于 Rapid，但双曲线样例产生缺少反斜杠的 `frac` 和重复减号。它适合作为可选复核后端，不适合无条件覆盖 Rapid。
- V2 先将 Rapid 最大解码从 512 降到 256 后，退化输入仍需约 31.9 秒才被拒绝；进一步采用 128-token 快速上限，把超长公式交给智能/精确路径，以换取更可控的最坏延迟。
- V3 写入代理未在约定时间内完成测试收敛；停止扩展后由主代理接管实际 diff、测试和交付验证。
- 删除完整编辑器后，旧 UI 测试仍导入 `main_window`，导致 pytest 收集失败；已按新的设置/教程/悬浮状态重写测试并恢复通过。
- 一次 `apply_patch` 同时删除和新增同一测试文件被拒绝；拆成两次明确补丁后完成。
- 首轮 V3 复审发现取消重新截图后旧结果不可恢复；已改为“取消保留，复制或主动关闭才消费”，并补充消费后不会复活的端到端测试。
- V4 独立审查发现未安装 Paddle 时仍可选择精确模式，以及预览脚本会继承用户主题；已禁用不可用选项、加载时回退智能模式，并强制预览主题确定性。

## Status
**Complete** - V4 设置中心、全局主题、圆环颜色盘、自定义 Logo、回归测试与预览验收均已完成。
