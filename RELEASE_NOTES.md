# FormulaSnip 0.2.11

- 内置模型在加载和选择轻量更新时完整校验 SHA-256，可写缓存仅保留诊断信息，防止同尺寸篡改绕过校验。
- 自动更新优先使用保留现有兼容模型的轻量安装包；模型缺失或版本不匹配时自动回退完整 Setup。
- 同时发布兼容旧客户端的 v1 完整安装清单与新版 v2 轻量更新清单；轻量包下载或校验失败时自动改用完整 Setup。
- 安装包最终 SHA-256 校验在后台下载任务完成，退出启动安装器时不再阻塞界面扫描大文件。
- 安装包通过校验后保持 Windows 禁写/禁删句柄直到启动，避免校验与执行之间被同尺寸覆盖。
- 统一等价的本地与 AI 识别结果，采用 AI 返回的紧凑 LaTeX 排版，同时保留真正不同结果的切换校核。
- 修复公式识别弹窗右上角关闭图标显示异常。
- Windows 安装版与便携版内置经过 SHA-256 校验的 MathCraft 公式模型，首次启动无需再下载模型。
- 升级安装保留用户已有的 MathCraft 缓存；内置模型完整时不会触发运行时重复下载。
- LaTeX 编辑、预览和 MathML 转换增加 4096 字符限制；超长输入整次拒绝。
- MathML 复制拒绝自定义宏定义，防止短公式经宏指数展开导致界面卡顿。
- 修复配置保存失败仍提示成功、无效 Logo 清空原设置及截图转换异常遗留忙状态的问题。
- 自动检查更新默认开启并遵循 12 小时节流，设置中可关闭；API 回退增加响应大小和总时限限制。
- Windows GUI 增加每用户单实例约束；异常日志统一脱敏，AI 遗留传输线程数量有界。
- 补充第三方许可证出处、隐私和安全说明，以及发行资源卫生检查。最终二进制与人工许可证/签名门禁仍需发布前验收。
