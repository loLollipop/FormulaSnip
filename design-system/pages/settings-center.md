# Page override: settings-center

- Base authority: ../MASTER.md
- Record only page-specific deviations here.
- The four-step usage flow uses theme-aware vector diagrams rather than text placeholders.
- Diagram sequence: click orb, drag-select one formula, verify the electronic result, copy into Word.
- Use one packaged `fx` brand asset consistently in the native title bar, taskbar, and sidebar.
- Mathematical examples must come from one validated LaTeX source and use real typesetting assets.
- The page header is a single 66 px row with title, theme action, and primary recognition action.
- Cards expose the task and current value first. Repeated explanations move to tooltips and
  accessibility descriptions.
- Persistent secondary copy is reserved for information required to make a decision. Dynamic
  progress, validation, error, and custom-selection feedback remain visible beside their control.
- Body text is 15 px and muted supporting text is at least 13 px; only version and code/value
  chips may use 12 px.
- Use 8 px / 12 px spacing increments, crisp card borders, moderate corners, and no gradients.
- The appearance page separates global UI accent from floating-orb ring color.
- Offer four global accents: signal blue (default), electric violet, aurora cyan,
  and mint teal. Every accent supports both light and dark modes.
- Accent changes apply immediately across the settings center, recognition result,
  update dialog, primary controls, focus states, toggles, and tutorial diagrams.
