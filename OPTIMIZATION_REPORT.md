# FormulaSnip MathCraft OCR 收敛报告

调研与本机验证日期：2026-09-15。

## 结论

FormulaSnip 现在固定使用 MathCraft OCR 0.3.1 的 CPU 公式识别路径。应用不再提供多个识别模式，也不运行候选比较。这样使启动预热、识别、配置、打包和错误处理都落在同一条可测试路径上。

```text
公式截图 → MathCraft OCR（ONNX Runtime CPU）→ 质量与预览检查 → 用户校对 → 复制
```

## 运行方式

- 公式 profile 在可复用的 spawn 子进程中运行，避免原生推理故障直接拖垮 GUI；
- 截图通过共享内存传输；
- Windows 发行包内置锁定的 MathCraft 公式模型，首次使用可以离线运行；源码模式或损坏回退可能下载模型；
- 发行包保留固定公式模型，排除其他 MathCraft、RapidOCR 和 ONNX Runtime 示例目录中的无用 `.onnx` 权重；
- `rapidocr` 是 MathCraft 的运行依赖，不是 FormulaSnip 的独立识别引擎，打包时必须保留其代码与必要资源。

## 质量策略

应用会检查括号、裸命令、重复运算符、异常长度、重复片段、低对比度、截图触边以及电子公式是否可预览。这些检查只能提示风险，不能替代模型置信度或人工校对。所有公式识别结果都应与原图核对。

应用不会默认增加全局二值化、锐化、放大或紧裁边，因为这些处理可能抹掉小数点、上下标、细分数线和积分上下限。只有在真实失败样本的消融测试证明有效后，才应增加针对性的图像处理。

## 验证边界与下一步

现有少量合成与真实截图可验证执行路径，但不足以量化总体准确率。后续应：

- 收集用户主动提供的错误原图和人工确认的正确 LaTeX；
- 建立 100–300 个许可明确的冻结样本，覆盖分式、根式、积分、求和、矩阵、分段、上下标、希腊字母、低清晰度和长公式；
- 报告字符串 exact、渲染等价、人工修正率、p50/p95 延迟和峰值内存；
- 在干净 Windows 环境验证打包后的 MathCraft 子进程、离线首次识别和模型损坏回退。
